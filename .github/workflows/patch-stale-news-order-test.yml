name: Patch stale news ordering test

on:
  push:
    branches:
      - feat/theme-dex-fossil

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/theme-dex-fossil
      - name: Update stale news ordering expectation
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('tests/collector-ui-contract.test.mjs')
          text = path.read_text(encoding='utf-8')
          old = 'assert.equal(newsData.items[0].id, "android-app-v09-update");'
          new = 'assert.equal(newsData.items[0].id, "iphone-pwa-install");'
          if old not in text:
              raise SystemExit('stale ordering expectation not found')
          path.write_text(text.replace(old, new, 1), encoding='utf-8')
          PY
          git rm .github/workflows/patch-stale-news-order-test.yml
          git add tests/collector-ui-contract.test.mjs
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git commit -m "Update stale news ordering test"
          git push origin HEAD:feat/theme-dex-fossil
