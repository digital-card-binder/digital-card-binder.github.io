"use strict";

(function () {
  // Legacy compatibility module.
  // Navigation is owned exclusively by collector-nav.js.
  // Keep this file inert so older cached pages cannot restore the retired menu.
  window.DigitalCardBinderLegacyThemeNavigationDisabled = true;
})();
