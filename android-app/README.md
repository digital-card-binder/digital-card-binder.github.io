# Digital Card Binder Android test

This lightweight Android launcher opens the live Digital Card Binder site in a Chrome Custom Tab so web updates are reflected without rebuilding the APK. The test build is generated by GitHub Actions and is not merged into the production branch until validation.
